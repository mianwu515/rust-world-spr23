service: random-fact-generator

provider:
  name: aws
  runtime: provided.al2

package:
  exclude:
    - ./**
  include:
    - ./bootstrap

functions:
  pizza-rust:
    handler: ./bootstrap
    events:
      - http:
          path: /fact
          method: GET